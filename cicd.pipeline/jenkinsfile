@Library('Mysharedlibrary') _
pipeline {
    agent any

    stages { 
        stage('Shared Function') {
            steps {
                mySharedFunction()
            }
        } 
        stage('Hello') {
            steps {
                echo "started the step"
                mySharedjenkinsfile()
            }
        }
    }
}